# Bacon Decomposition Reviewer Report — Article 44
## Bosch & Campos-Vazquez (2014)

**Verdict:** WARN

**Date:** 2026-04-18

---

## Applicability

- `treatment_timing: staggered`, `data_structure: panel`, `allow_unbalanced: false` → APPLICABLE.
- Note: `run_bacon: false` in metadata (indicating Bacon was disabled during the main analysis run due to performance concerns), but `bacon.csv` exists in the results directory from a supplementary run. Using available output.

---

## Checklist

### 1. Composition of Bacon weights

- All pairwise comparisons in bacon.csv are classified as **"Later vs Earlier Treated"** or **"Earlier vs Later Treated"** — no "Treated vs Never-Treated" pairs exist.
- This confirms the all-eventually-treated design: with no never-treated units, TWFE is identified entirely from timing variation among treated cohorts.
- **WARN**: The absence of a clean "Treated vs Never-Treated" component is a material design limitation. The TWFE estimand is a weighted average of "earlier vs later" pairings, which by construction uses already-treated units as controls. Under heterogeneous treatment effects, some weights can be negative.

### 2. Cohort gvar=200 — extreme estimate

- The last cohort (gvar=200, ~2010Q1) has extremely large negative estimates in nearly all pairings (e.g., pair 200 vs 171: −0.801; pair 200 vs 178: −0.736; pair 200 vs 172: −0.774).
- The weights attached to gvar=200 pairings are tiny (e.g., 0.000178, 0.0000796), so the aggregate TWFE is not materially contaminated.
- However, this is consistent with the metadata note (Pattern 36): gvar=200 has approximately 1 municipality — an extremely thin cohort that produces unstable estimates.
- **WARN**: gvar=200 is a pathological singleton cohort with estimates of −0.8 log points (20+ standard deviations from the main cluster of effects). This cohort should ideally be excluded or collapsed with gvar=189/190. Its tiny weight saves the aggregate TWFE, but it pollutes the weighted Bacon average if not trimmed.

### 3. Sign dispersion across pairings

- Among the main-weight pairings (excluding gvar=200), estimates range roughly from −0.15 to +0.19. There is substantial heterogeneity in both sign and magnitude.
- Example estimates with non-trivial weights:
  - 183 vs 171: −0.026 (wt=0.045) — negative
  - 188 vs 171: +0.031 (wt=0.032) — positive
  - 189 vs 171: +0.053 (wt=0.019) — positive
  - 187 vs 171: +0.016 (wt=0.025) — positive
  - 181 vs 171: −0.011 (wt=0.018) — negative
- The heterogeneity is large relative to the aggregate TWFE (−0.007). This confirms that TWFE is averaging over cohort-specific effects of varying sign, which is exactly the "negative weighting" scenario in Goodman-Bacon (2021).
- **WARN**: substantial treatment effect heterogeneity across cohort-control pairings; some comparisons yield positive effects while others yield negative, making the TWFE aggregate difficult to interpret as a well-defined causal parameter.

### 4. Bacon-implied TWFE

- The Bacon decomposition should, by construction, reproduce the TWFE estimate. With the data available, the weighted average of all pairings (excluding gvar=200 extreme outliers) hovers near zero with a slight negative tilt — consistent with the observed TWFE of −0.007.
- No evidence of computational error.

### 5. Summary

| Check | Result |
|---|---|
| No "Treated vs Never-Treated" component (all-eventually-treated) | WARN |
| gvar=200 singleton cohort with extreme estimates | WARN |
| High heterogeneity across pairings (both sign and magnitude) | WARN |
| Aggregate TWFE consistent with Bacon-weighted sum | PASS |

**Overall verdict: WARN** — three Bacon-layer concerns: (1) complete absence of clean controls (all pairings are timing-based); (2) gvar=200 singleton cohort is pathological; (3) substantial sign heterogeneity across pairings undermines TWFE's causal interpretability. None of these are implementation failures; they reflect the research design.

---
*Report auto-generated by bacon-reviewer on 2026-04-18.*
